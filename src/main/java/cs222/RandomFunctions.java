package cs222;

public class RandomFunctions {
    public boolean isEvenNumber (int input){
        return (input % 2 == 0);
    }
    public boolean isAtLeastTenChar (String input){
        return (input.length() > 10);
    }
}
